<%= form_with(model: @center, local: true) do |f| %>
  <%= render 'shared/error_messages_center', object: @center %>

  <%= f.label :number, class: "label-#{yield(:class_text)}" %>
  <%= f.text_field :number, class: "form-control" %>

  <%= f.label :name, class: "label-#{yield(:class_text)}" %>
  <%= f.text_field :name, class: "form-control" %>

  <%= f.label :attendance_type, class: "label-#{yield(:class_text)}" %>
  <%= f.text_field :attendance_type, class: "form-control" %>

  <%= f.submit yield(:button_text), class: "btn btn-primary btn-block btn-#{yield(:class_text)}" %>
  <%= link_to "キャンセル", centers_path, class: "btn btn-default btn-block" %>
<% end %>